# 策略模式 (Strategy Pattern) 标准示例

## 📚 概述

策略模式定义了一系列算法，把它们封装起来，并使它们可以相互替换。该模式使得算法可以独立于使用它的客户端而变化。

## 🏗️ 结构

### 核心组件

1. **Strategy (策略接口)** - `PaymentStrategy`
   - 定义所有具体策略的公共接口

2. **ConcreteStrategy (具体策略)** 
   - `CreditCardPayment` - 信用卡支付
   - `AliPayPayment` - 支付宝支付
   - `WeChatPayment` - 微信支付
   - `PayPalPayment` - PayPal支付

3. **Context (上下文)** - `ShoppingCart`
   - 持有策略的引用
   - 可以动态切换策略

## 📋 类图

```
┌─────────────────────┐
│  PaymentStrategy    │ ← 策略接口
│  + pay(amount)      │
└─────────────────────┘
          ↑
          │ implements
          │
    ┌─────┴─────┬────────┬─────────────┐
    │           │        │              │
┌───┴───┐ ┌───┴──┐ ┌──┴──────┐ ┌───┴──────┐
│Credit │ │AliPay│ │WeChat   │ │PayPal    │ ← 具体策略
│Payment│ │Pay   │ │Payment  │ │Payment   │
└───┬───┘ └───┬──┘ └───┬─────┘ └───┬──────┘
    │         │        │           │
    └─────────┴────────┴───────────┘
              ↓
      ┌───────────────┐
      │ ShoppingCart  │ ← 上下文
      │ - strategy    │
      │ + checkout()  │
      └───────────────┘
```

## ✨ 特点

### 优势
1. **开闭原则**: 对扩展开放，对修改封闭。新增支付方式无需修改现有代码
2. **消除条件语句**: 避免使用大量的 if-else 语句
3. **运行时切换**: 可以在运行时动态改变策略
4. **算法独立**: 策略之间完全独立，互不影响

### 劣势
1. 客户端必须了解所有策略之间的差异
2. 会增加类的数量

## 🎯 适用场景

- 一个类有多种行为方式，并且想要动态选择使用哪种方式
- 避免使用多重的条件选择语句
- 有许多相关的类，它们之间的差别仅在于行为不同
- 算法的使用和算法的实现分离

## 💡 使用示例

```java
// 创建购物车
ShoppingCart cart = new ShoppingCart();
cart.addItem("商品1", 100);
cart.addItem("商品2", 200);

// 动态切换支付策略
cart.setPaymentStrategy(new CreditCardPayment("1234", "张三"));
cart.checkout();

cart.setPaymentStrategy(new AliPayPayment("1388888"));
cart.checkout();
```

## 🚀 运行测试

```bash
mvn compile exec:java -Dexec.mainClass="com.skuu.design.strategy.standard.StrategyPatternTest"
```

## 📝 与模板方法模式的区别

- **模板方法模式**: 在父类中定义算法的骨架，子类实现具体步骤
- **策略模式**: 封装算法族，使它们可以互换使用

